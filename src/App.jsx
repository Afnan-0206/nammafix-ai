import { useEffect, useState } from 'react'
import { Navigate, Route, Routes, useLocation } from 'react-router-dom'
import Navbar from './components/Navbar'
import MobileBottomNav from './components/MobileBottomNav'
import LoadingScreen from './components/LoadingScreen'
import PresentationMode from './components/PresentationMode'
import Onboarding from './components/Onboarding'
import InstallPrompt from './components/InstallPrompt'
import ResetModal from './components/ResetModal'
import { useToast } from './components/Toast'
import Home from './pages/Home'
import ReportIssue from './pages/ReportIssue'
import Dashboard from './pages/Dashboard'
import IssueDetail from './pages/IssueDetail'
import AuthorityConsole from './pages/AuthorityConsole'
import Impact from './pages/Impact'
import Leaderboard from './pages/Leaderboard'
import DemoFlow from './pages/DemoFlow'
import { loadIssues, persistIssues } from './utils/issues'
export default function App() { const [issues, setIssues] = useState(loadIssues); const [showReset, setShowReset] = useState(false); const [loading, setLoading] = useState(true); const location = useLocation(); const { showToast } = useToast(); useEffect(() => { try { persistIssues(issues) } catch(e) { if (e.name === 'QuotaExceededError') { const pruned = issues.slice(0, issues.length - 2); persistIssues(pruned); setIssues(pruned); showToast({ type: 'info', title: 'Storage limit reached', body: 'Oldest 2 reports removed to make space.' }) } } }, [issues]); useEffect(() => { const sections = [...document.querySelectorAll('#main-content main > section')]; const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches; if (reduced || !('IntersectionObserver' in window)) { sections.forEach((section) => section.classList.add('is-visible')); return undefined } sections.forEach((section, index) => { section.classList.add('reveal-section'); section.style.transitionDelay = `${index * 60}ms` }); const observer = new IntersectionObserver((entries) => entries.forEach((entry) => { if (entry.isIntersecting) { entry.target.classList.add('is-visible'); observer.unobserve(entry.target) } }), { threshold: .15, rootMargin: '0px 0px -50px 0px' }); sections.forEach((section) => observer.observe(section)); return () => observer.disconnect() }, [location.pathname]); const createIssue = (issue) => { setIssues((current) => [issue, ...current]); showToast({ type: 'success', title: 'Report filed · AI analysis ready', body: 'Your report was saved in this browser.' }); if (issue.possibleDuplicates?.length) showToast({ type: 'warning', title: 'Similar report already exists', body: issue.possibleDuplicates[0].title }) }; const updateIssue = (id, patch) => { const before = issues.find((issue) => issue.id === id); setIssues((current) => current.map((issue) => issue.id === id ? { ...issue, ...patch } : issue)); if (patch.status && before?.status !== patch.status) showToast({ type: 'info', title: `Status updated to ${patch.status}`, body: 'Saved in this browser only.' }) }; const verifyIssue = (id) => { const issue = issues.find((item) => item.id === id); if (issue?.userVerifications) { showToast({ type: 'warning', title: 'Already verified by you', body: 'One local verification is enough for this demo.' }); return } setIssues((current) => current.map((item) => item.id === id ? { ...item, verifications: item.verifications + 1, userVerifications: 1, status: item.status === 'Reported' ? 'Verified' : item.status } : item)); showToast({ type: 'success', title: 'Issue verified', body: '+ 15 demo points earned' }) }; const reset = () => { localStorage.removeItem('nammafix-issues-v1'); window.location.assign('/') }; return <div className="min-h-screen bg-ink text-slate-100">{loading && <LoadingScreen onDone={() => setLoading(false)} />}<Navbar /><PresentationMode /><Onboarding /><InstallPrompt /><div id="main-content" className="page-enter" key={location.pathname}><Routes><Route path="/" element={<Home issues={issues} />} /><Route path="/report" element={<ReportIssue onCreate={createIssue} issues={issues} />} /><Route path="/dashboard" element={<Dashboard issues={issues} onVerify={verifyIssue} />} /><Route path="/issues/:issueId" element={<IssueDetail issues={issues} onVerify={verifyIssue} onUpdate={updateIssue} />} /><Route path="/authority" element={<AuthorityConsole issues={issues} onUpdate={updateIssue} />} /><Route path="/impact" element={<Impact issues={issues} />} /><Route path="/leaderboard" element={<Leaderboard issues={issues} />} /><Route path="/demo-flow" element={<DemoFlow />} /><Route path="*" element={<Navigate to="/404" replace />} /><Route path="/404" element={<NotFound />} /></Routes></div><MobileBottomNav /><footer className="border-t border-line bg-panel/40 py-6 text-center"><div className="mx-auto border-l-2 border-civic px-5"><p className="font-display text-sm font-semibold text-slate-300">NammaFix AI · Functional hackathon MVP</p><p className="mt-1 font-mono text-[11px] text-amber">Prototype Mode · All data lives in your browser · Nothing is sent anywhere</p><button className="reset-link mt-3" onClick={() => setShowReset(true)}>Reset demo data</button></div></footer>{showReset && <ResetModal onClose={() => setShowReset(false)} onConfirm={reset} />}</div> }
function NotFound() { return <main className="civic-grid grid min-h-[70vh] place-items-center px-4 text-center"><div><div className="relative mx-auto w-fit"><p className="font-display text-[120px] font-extrabold leading-none text-line">404</p><span className="absolute left-1/2 top-1/2 grid h-10 w-10 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full bg-civic text-xl text-ink">⌖</span></div><h1 className="heading mt-4 text-3xl">This road doesn't exist yet.</h1><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-slate-400">The issue you're looking for hasn't been filed — or was resolved and archived.</p><div className="mt-6 flex flex-wrap justify-center gap-3"><a className="btn-primary" href="/">Go home</a><a className="btn-secondary" href="/report">Report a new issue</a></div></div></main> }
