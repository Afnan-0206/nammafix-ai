import { useEffect, useRef, useState } from 'react'
export default function CountUp({ value, duration = 1000 }) { const [shown, setShown] = useState(0); const [complete, setComplete] = useState(false); const ref = useRef(null); useEffect(() => { const element = ref.current; if (!element) return undefined; const run = () => { if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) { setShown(value); setComplete(true); return } const start = performance.now(); let frame; const tick = (time) => { const progress = Math.min((time - start) / duration, 1); setShown(Math.round(value * (1 - (1 - progress) ** 3))); if (progress < 1) frame = requestAnimationFrame(tick); else setComplete(true) }; frame = requestAnimationFrame(tick); return () => cancelAnimationFrame(frame) }; if (!('IntersectionObserver' in window)) return run(); const observer = new IntersectionObserver(([entry]) => { if (entry.isIntersecting) { run(); observer.disconnect() } }, { threshold: .5 }); observer.observe(element); return () => observer.disconnect() }, [value, duration]); return <span ref={ref} className={complete ? 'countup-final' : ''}>{shown}</span> }
