import { BarChart3, FilePlus2, Home, LayoutDashboard, Menu, ShieldCheck, Trophy, Workflow, X } from 'lucide-react'
import { NavLink } from 'react-router-dom'
import { useState } from 'react'
const mainTabs = [[Home, 'Home', '/'], [FilePlus2, 'Report', '/report'], [LayoutDashboard, 'Dashboard', '/dashboard'], [BarChart3, 'Impact', '/impact']]
const moreTabs = [[ShieldCheck, 'Authority Prototype', '/authority'], [Trophy, 'Scoreboard', '/leaderboard'], [Workflow, 'Demo Flow', '/demo-flow']]
export default function MobileBottomNav() { const [moreOpen, setMoreOpen] = useState(false); return <><nav className="mobile-bottom-nav" aria-label="Mobile navigation">{mainTabs.map(([Icon, label, path]) => <NavLink key={path} to={path} className={({ isActive }) => `mobile-tab ${isActive ? 'mobile-tab-active' : ''}`}><Icon size={20} /><span>{label}</span></NavLink>)}<button className={`mobile-tab ${moreOpen ? 'mobile-tab-active' : ''}`} onClick={() => setMoreOpen(true)} aria-label="Open more navigation"><Menu size={20} /><span>More</span></button></nav>{moreOpen && <div className="mobile-sheet-backdrop" onMouseDown={() => setMoreOpen(false)}><section className="mobile-sheet" onMouseDown={(event) => event.stopPropagation()}><div className="flex items-center justify-between"><p className="font-display font-bold text-slate-100">More</p><button onClick={() => setMoreOpen(false)} aria-label="Close more navigation" className="rounded-lg p-2 text-slate-400 hover:bg-raised"><X size={18} /></button></div><div className="mt-4 grid gap-2">{moreTabs.map(([Icon, label, path]) => <NavLink key={path} to={path} onClick={() => setMoreOpen(false)} className="flex items-center gap-3 rounded-lg px-3 py-3 text-sm font-medium text-slate-300 hover:bg-raised hover:text-civic"><Icon size={18} />{label}</NavLink>)}</div></section></div>}</> }
