import { useEffect, useState } from 'react'
export default function LoadingScreen({ onDone }) { const [leaving, setLeaving] = useState(false); useEffect(() => { const startExit = setTimeout(() => setLeaving(true), 1000); const done = setTimeout(onDone, 1200); return () => { clearTimeout(startExit); clearTimeout(done) } }, [onDone]); return <div className={`loading-screen ${leaving ? 'loading-leave' : ''}`} aria-label="Loading NammaFix AI"><div className="text-center"><div className="flex items-center justify-center gap-2 font-display text-2xl font-bold"><svg aria-hidden="true" width="30" height="30" viewBox="0 0 24 24" fill="none" className="text-civic"><path d="m12 2 8 4.5v9L12 20l-8-4.5v-9L12 2Z" stroke="currentColor" strokeWidth="1.7" /><path d="M8 12h8M12 8v8" stroke="currentColor" strokeWidth="1.7" strokeLinecap="round" /></svg><span><span className="text-civic">Namma</span><span className="text-slate-100">Fix AI</span></span></div><div className="loading-track mt-5"><span /></div></div></div> }
