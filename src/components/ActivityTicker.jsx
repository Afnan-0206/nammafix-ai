import { ChevronRight } from 'lucide-react'
import { useEffect, useState } from 'react'
const activity = ["Rajesh K. verified ‘Pothole near Agara Farm Junction’ · 2 min ago", "New report filed: ‘Broken footpath on Varthur Road’ · 5 min ago", "BBMP Whitefield marked ‘Open drainage’ as Resolved · 12 min ago", "3 new verifications on ‘Water leakage near school road’ · 18 min ago"]
export default function ActivityTicker() { const [index, setIndex] = useState(0); const [paused, setPaused] = useState(false); useEffect(() => { if (paused) return undefined; const timer = setInterval(() => setIndex((value) => (value + 1) % activity.length), 4000); return () => clearInterval(timer) }, [paused]); return <div className="activity-ticker" onMouseEnter={() => setPaused(true)} onMouseLeave={() => setPaused(false)}><span className="flex shrink-0 items-center gap-2 font-mono text-[11px] font-semibold text-civic"><span className="activity-dot" />Live activity</span><p key={index} className="activity-message">● {activity[index]}</p><span className="ml-auto flex shrink-0 items-center text-xs text-slate-400">View all <ChevronRight size={14} /></span></div> }
